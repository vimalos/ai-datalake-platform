# Environment Configuration Template for AI Data Lakehouse Platform
# Copy this file to .env and update with your values

# ============================================
# AWS Configuration
# ============================================
AWS_REGION=us-east-1
AWS_GLUE_ENABLED=true
AWS_GLUE_CATALOG_ID=
AWS_S3_BUCKET=datalake-xxxx-dev
AWS_S3_WAREHOUSE_PATH=analytics

# For EC2 deployment, use IAM instance role (recommended)
# For local development with MinIO or S3, set these:
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_ENDPOINT=http://localhost:9000  # For MinIO

# ============================================
# Spark Configuration
# ============================================
SPARK_ENABLED=true
SPARK_MASTER=local[*]
METASTORE_TYPE=glue
SPARK_WAREHOUSE=s3a://datalake-xxx-dev

# For local development with Hive:
# METASTORE_TYPE=hive
# HIVE_METASTORE_URI=thrift://localhost:9083
# SPARK_WAREHOUSE=/tmp/spark-warehouse

# ============================================
# LLM Configuration
# ============================================
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3
OLLAMA_EMBEDDING_MODEL=llama3

# Optional: Claude API
# CLAUDE_ENABLED=false
# ANTHROPIC_API_KEY=your-anthropic-key

# ============================================
# MCP Configuration
# ============================================
MCP_CLIENT_ENABLED=true
# Format: name1|url1,name2|url2
MCP_REMOTE_SERVERS=

# ============================================
# CORS Configuration
# ============================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# For EC2 deployment, add your public IP:
# CORS_ALLOWED_ORIGINS=http://YOUR_EC2_PUBLIC_IP,http://YOUR_DOMAIN

# ============================================
# Server Configuration
# ============================================
SERVER_PORT=8080

# ============================================
# Milvus (Optional - for advanced RAG)
# ============================================
MILVUS_HOST=localhost
MILVUS_PORT=19530

# ============================================
# Iceberg Configuration
# ============================================
ICEBERG_CATALOG_URI=http://iceberg-rest:8181
ICEBERG_CATALOG_TYPE=rest

# NOTE: PostgreSQL is used by Hive Metastore in local development
# but is NOT used by the Spring Boot application itself

# Frontend Configuration
REACT_APP_API_URL=http://localhost:8080/api

# Spark Configuration (for distributed execution)
SPARK_MASTER=local[*]
SPARK_EXECUTOR_MEMORY=4g
SPARK_DRIVER_MEMORY=2g
SPARK_EXECUTOR_CORES=4

# Logging Configuration
LOG_LEVEL=DEBUG

